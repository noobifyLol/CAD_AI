{
  "fixtures": [
    {
      "name": "spurGear_typedLambda_and_incomplete_involute",
      "expectStrictPass": false,
      "code": "FeatureScript 2931;\nimport(path : \"onshape/std/geometry.fs\", version : \"2931.0\");\n\nannotation { \"Feature Type Name\" : \"Spur Gear\" }\nexport const spurGear = defineFeature(function(context is Context, id is Id, definition is map)\n    precondition\n    {\n        annotation { \"Name\" : \"Plane\" }\n        definition.location is Query;\n        annotation { \"Name\" : \"Number of Teeth\" }\n        isInteger(definition.numTeeth, { (unitless) : [10, 10, 100] } as IntegerBoundSpec);\n        annotation { \"Name\" : \"Pitch Radius\" }\n        isLength(definition.pitchRadius, LENGTH_BOUNDS);\n        annotation { \"Name\" : \"Pressure Angle (deg)\" }\n        isInteger(definition.pressureAngleDeg, { (unitless) : [20, 20, 30] } as IntegerBoundSpec);\n        annotation { \"Name\" : \"Face Width\" }\n        isLength(definition.faceWidth, LENGTH_BOUNDS);\n    }\n    {\n        // Fix: Correct usage of evPlane with a face map\n        var skPlane = isQueryEmpty(context, definition.location)\n            ? plane(WORLD_ORIGIN, Z_DIRECTION)\n            : evPlane(context, { \"face\" : definition.location });\n\n        var sk = newSketchOnPlane(context, id + \"sk\", { \"sketchPlane\" : skPlane });\n        skSolve(sk);\n\n        var pitchRadius = definition.pitchRadius;\n        var pressureAngleRad = definition.pressureAngleDeg * PI / 180;\n        var baseRadius = pitchRadius * cos(pressureAngleRad);\n        var rootRadius = max(pitchRadius - 1.25 * pitchRadius / definition.numTeeth, pitchRadius * 0.5);\n        var tipRadius = pitchRadius + pitchRadius / definition.numTeeth;\n\n        skCircle(sk, \"pitch\", { \"center\" : vector(0, 0) * inch, \"radius\" : pitchRadius });\n        skCircle(sk, \"base\", { \"center\" : vector(0, 0) * inch, \"radius\" : baseRadius });\n        skCircle(sk, \"root\", { \"center\" : vector(0, 0) * inch, \"radius\" : rootRadius });\n        skCircle(sk, \"tip\", { \"center\" : vector(0, 0) * inch, \"radius\" : tipRadius });\n\n        const involute = function(t, rb) {\n\n        };\n\n        var points = [];\n        for (var i = 0; i < 10; i += 1) {\n            var t = i * 0.1;\n            points.push(involute(t, baseRadius));\n        }\n\n        opExtrude(context, id + \"tooth\", {\n            \"entities\"  : qSketchRegion(id + \"toothProfile\"),\n            \"direction\" : skPlane.normal,\n            \"endBound\"  : BoundingType.BLIND,\n            \"endDepth\"  : definition.faceWidth\n        });\n    });"
    },
    {
      "name": "restaurantMenu_execution_in_precondition",
      "expectStrictPass": false,
      "code": "FeatureScript 2931;\nimport(path : \"onshape/std/geometry.fs\", version : \"2931.0\");\n\nannotation { \"Feature Type Name\" : \"Custom Restaurant Menu\" }\nexport const customMenu = defineFeature(function(context is Context, id is Id, definition is map)\n    precondition\n    {\n        annotation { \"Name\" : \"Plane\" }\n        definition.location is Query;\n        annotation { \"Name\" : \"Width\" }\n        isLength(definition.width, LENGTH_BOUNDS);\n        var sk = newSketchOnPlane(context, id + \"sk1\", { \"sketchPlane\" : skPlane });\n        skRectangle(sk, \"rect1\", { \"firstCorner\" : vector(-(definition.width / inch) / 2, -(definition.height / inch) / 2) * inch, \"secondCorner\" : vector((definition.width / inch) / 2, (definition.height / inch) / 2) * inch });\n        skSolve(sk);\n        opExtrude(context, id + \"ext1\", {\n            \"entities\"  : qSketchRegion(id + \"sk1\"),\n            \"direction\" : skPlane.normal,\n            \"endBound\"  : BoundingType.BLIND,\n            \"endDepth\"  : definition.depth\n        });\n    }\n    {\n    });"
    },
    {
      "name": "bandAid_missing_skeleton_structure",
      "expectStrictPass": false,
      "code": "FeatureScript 2931;\nimport(path : \"onshape/std/geometry.fs\", version : \"2931.0\");\n\nannotation { \"Feature Type Name\" : \"Band Aid\" }\nexport const bandAid = defineFeature(function(context is Context, id is Id, definition is map)\n    precondition\n    {\n        annotation { \"Name\" : \"Plane\", \"Filter\" : GeometryType.PLANE, \"MaxNumberOfPicks\" : 1 }\ndefinition.location is Query;\n\n        annotation { \"Name\" : \"Width\", \"Default\" : \"1 * inch\" }\nisLength(definition.width, LENGTH_BOUNDS);\n    }\n    {\n    });"
    }
  ]
}

